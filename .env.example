# ---- M6.3 MCP 服务器（可选；JSON 列表，可多个；不可达不阻断启动）----
# 示例（Obsidian Local REST API 插件，启用 MCP server 后）：
# MCP_SERVERS=[{"serverName":"obsidian","url":"http://127.0.0.1:27123/mcp/","headers":{"Authorization":"Bearer <API_KEY>"},"prefix":"obsidian"}]
MCP_SERVERS=
